# OMP Config Doctor

Run `omp_config_doctor` and report hardcoded home-path portability risks in the packaged config template.

Do not modify `~/.omp` unless the user explicitly asks for a patch plan and reviews it.
