# OMP Enhancer 本轮不纳入内容

日期：2026-07-07
状态：已存档，后续另起任务处理

以下内容不纳入本轮工作流和门禁重构。

## 1. 自动优化 prompt

本轮只实现 route card、门禁恢复、重复检测和 debug 日志，不做自动 prompt 优化。

后续如果要做，应先建立独立评估集，再做候选 prompt 的保留或回退机制。

## 2. 自动改 skill 文件

本轮不让 agent 自动修改 skill 文件。

后续如果要做，应先把失败会话转成测试样例，再人工确认 skill 修改策略。

## 3. 默认调用真实模型

本轮默认测试只使用 fixture 和 fake model。

真实模型评估保留为可选命令，不进入默认测试和 CI。

## 4. 复杂 UI 面板

本轮只写 JSONL debug 日志，不做可视化面板。

后续如果需要 UI，应先稳定日志 schema。

## 5. 数据库存储 gate 日志

本轮日志只写本地 JSONL。

后续如果要进数据库，应先确认字段、索引和清理策略。

## 6. 把所有旧测试一次性重写

本轮新增 workload matrix 和关键回归测试。

旧测试只在与新方案冲突时逐步调整，不一次性重写。

## 下次处理建议

这些内容应拆成单独任务。不要混入本轮重构，避免扩大范围。