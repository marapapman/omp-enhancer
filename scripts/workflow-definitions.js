import { defineWorkflowCatalog } from './workflow-schema.js';
export const WORKFLOW_CATALOG_VERSION = 44;
const writingWorkflows = [
  {
    id: 'writing',
    chooseWhen: 'Prose and copy in any language (English, Chinese) or format (LaTeX, Markdown, Beamer, Word, PowerPoint/PPTX): the dedicated text tools writer/zh-writer author every wording surface, while Main/task handle code, evidence, structure, layout, conversion, and validation.',
    skills: [
      'writing-review',
      'format-humanizer',
      'plain-chinese-writing',
      'zh-research-achievement-writing',
      'zh-format-humanizer',
      'zh-writing-review',
      'zh-writing-polish',
      'writing-markdown-helper',
      'zh-writing-markdown-helper',
      'format-markdown2latex',
      'format-latex2markdown',
      'format-template-latex',
      'latex-beamer-slides',
      'beamer-to-powerpoint',
      'slides-storyline',
      'docx',
    ],
    catalogSkills: [],
    roles: ['writer', 'zh-writer', 'checker', 'zh-checker', 'task'],
    suggestedFlow: [
      'Identify target language (zh/en) and format (plain/LaTeX/Markdown/Beamer/Word/PowerPoint/PPTX).',
      'Load matching language and format skills.',
      'The substantive-text sequence starts with a proposal from the language-matched dedicated text tool, writer (English) or zh-writer (Chinese), covering drafting, logical/semantic revision, translation, polishing, titles, labels, captions, notes, and UI copy; mixed-language content uses separate writer and zh-writer text-tool calls by segment; Main preserves semantic anchors and integrates the returned proposal verbatim.',
      'Then checker/zh-checker reports logic/evidence issues without rewriting prose; any substantive repair returns to the same language-matched text tool. Main integrates exact writer output, while Main/task/checker never rewrite prose; code, evidence collection, structural reordering, layout, conversion, validation, and other non-text actions may remain with Main/task.',
      'If the writer text tool cannot safely handle substantive English prose (or zh-writer Chinese prose), record that inability as a limitation rather than silently drafting through another role.',
      'For Word output, apply the approved writer/zh-writer content through officecli (docx skill) and verify with officecli validate and a rendered view; task may perform only structural, layout, conversion, and validation operations and must not change wording.',
      'For new Beamer decks, the writer/zh-writer text tool produces the Markdown content plan whose titles avoid the "XX：XX" label+colon+label pattern and read as coherent prose in deck order; confirm it with the user, let a separate task reconcile slide order and structure, then have the task convert the unchanged text-tool output into Beamer and begin layout.',
      'After either a new or existing Beamer deck reaches its final validated Beamer visual revision, branch optionally to PPTX output when requested: use the fixed external `beamer2pptx` Skill/repository at https://github.com/xdmlxdml/beamer2pptx/tree/main/beamer2pptx with the final validated PDF and corresponding `.tex`, macro, and font sources when available; a producing task performs only PPTX conversion, binding, rendering, and layout-only fixes, then one independent read-only reviewer checks the current PPTX renders. The producing task may apply at most one bounded layout-only fix, rerender fresh evidence, and the same reviewer confirms only those findings once. Keep this branch advisory with no automatic loop or hard gate; preserve visible content, formulas, slide order, and Markdown/Beamer sources, and return content or page-structure issues to the writer/zh-writer path, then to the Markdown plan and Beamer regeneration path.',
      'For substantial de-AI work, apply the format-humanizer/zh-format-humanizer rules one at a time before checking: a scan task inventories the tell families present, then one writer/zh-writer text-tool pass per found family runs strictly sequentially — Main verifies each pass output\'s semantic-anchor check and hands it to the next pass; all rewriting stays with the text tools and no pass mixes families.',
      'Check via checker/zh-checker for substantial work to report logic, semantic, and evidence issues; Main checks orchestration, preservation, and consistency only and never authors or edits text.',
      'Deliver with preservation and consistency verification.',
    ],
    scopeNotes: [
      'Language selection: detect the target body rather than the instruction language; route English text to the dedicated text tool writer and Chinese text to the dedicated text tool zh-writer, using both for mixed-language content.',
      'LaTeX/Beamer/Word/Markdown are format overlays, not separate workflows; select matching format skills.',
      'Word (.docx) documents are produced and edited through officecli (single binary, no Office install): create/draft content via the language-matched text tool (writer/zh-writer), apply it with officecli add/set, then verify with officecli validate and view issues, and render view html/screenshot for visual confirmation before delivery. officecli also covers .xlsx and .pptx, and its merge command fills {{key}} templates from JSON.',
      'Every English or Chinese prose/copy surface is authored only by its language-matched dedicated text tool, keeping text authorship writer-only: drafting, revision, translation, polishing, titles, labels, captions, notes, narrative copy, and UI copy start with a writer/zh-writer proposal; Main preserves semantic anchors and integrates the returned text verbatim; checker/zh-checker reports logic/evidence issues without rewriting prose; any substantive repair returns to the same language-matched text tool. Main/task own code, evidence collection, structural reordering, layout, conversion, validation, and other non-text actions and never rewrite prose.',
      'If the language-matched text tool cannot safely handle substantive prose, record the inability as a limitation and do not silently draft through another role.',
      'For new Beamer decks, start with a text-only Markdown content plan produced by the writer/zh-writer text tool, discuss and confirm each page with the user, then launch a separate task to reconcile the slide order on the unchanged plan (no content overlap, semantically coherent modules, logical overall progression; page titles avoid the "XX：XX" label+colon+label pattern and all titles read as coherent prose in deck order; crossings that reordering or regrouping cannot fix return to Markdown reconfirmation) before the task converts the plan into Beamer and begins visual authoring and basic layout. The Markdown content plan is the canonical content source owned by the text tools and the Beamer .tex files are derived layout artifacts; content changes go back to writer/zh-writer, who updates Markdown first; after user reconfirmation, regenerate Beamer before layout resumes.',
      'Chinese slide copy uses plain-chinese-writing for natural sentences, zh-format-humanizer for AI-like phrasing, and zh-writing-review for page-level clarity; English slide copy uses format-humanizer for AI-tell removal and writing-review for page-level review; use zh-writing-polish only for actual polishing and never replace body prose with keyword or phrase lists.',
      'De-AI is one-sighting in every language: any listed tell justifies an edit on its first appearance — prefer an over-correction to a miss ("宁可误杀，不可放过"). Only quotations, titles, proper names, passages discussing a phrase rather than using it, text predating November 30, 2022, and details that carry the writer\'s voice (specific unusual details, mixed feelings, first-person choices) are exempt.',
      'De-AI application is per-rule, not one mixed pass: for substantial documents, first run one scan task that inventories which tell families actually appear (contrast-repetition, one-line closers, sayings that sound deep, staged run-up, arguing with no one, forced triads, repeated openings, dashes, stacked qualifiers, hyphenated pairs, passive/missing subjects, symmetrical paragraphs, inflated words, inflated significance, -ing riders, sales language, borrowed authority, vague connection, copula avoidance, bold decoration, decorative headings, curly quotes, chatbot residue, knowledge-limit disclaimers, repeated headings, writing about previous versions; zh: 否定式对偶、单句收尾、貌似深刻的说法、铺垫式开场、与不存在的对手辩论、意义拔高、理解承诺句、抽象复述回声、宣布式过渡、节奏对仗收尾、及弱特征清单), then run one writer/zh-writer text-tool pass per found tell family strictly sequentially. Main verifies each pass output\'s semantic-anchor check (限定/模态/范围/否定/方向/数字/引用 unchanged; no fact added or dropped) and hands its output to the next pass before the next pass runs. Each pass handles only its one family on the full current text and returns SEARCH/REPLACE blocks or a diff; Main owns file effects. Skip families the scan did not find.',
      'Hard phrasing bans in every deliverable (prose, slides, figures, documents): no "不是X，而是Y" contrast-repetition construction or its English equivalents ("not X, but Y", "not just X") — state the claim once, directly, with no restatement in inverted or synonym form; no opening or closing one-sentence summary that restates what the text just showed; and, in structured artifacts, no "XX：XX" label+colon+label titles or label-colon bullet lead-ins. These are fixed whenever found, not clustering-gated tells. Verbatim quotations and cited material are exempt.',
      'No "specifics then sweep" summary clause: do not follow a concrete factual clause with a summarizing clause that elevates it to a sweeping whole ("……。这些共同构成了……/这一切标志着……/正是这些……成就了……"; English "Together, these ...", "All of this marked ...", "It was these ... that ..."). The tell has two faces: the second clause adds no new fact, only inflation; and its skeleton mirrors the first clause (same object, scope adverb, agent, completion verb), forming a loose pseudo-parallel couplet. This is the documentary-narration "画面＋全称升华" two-beat sentence — a stock template of AI-written popular history. Fix by keeping the concrete clause and cutting the sweep, or by replacing the sweep with a real fact.',
      'No "understanding promise" transitions ("理解了 X，Y 就好懂了", "把这个说清楚，A、B 和 C 就容易理解了", "X is the key to understanding Y"): an empty promise about the reader\'s future comprehension with no stated dependency — delete it, or state the real dependency concretely ("A、B、C 都以 X 为基础"), or, when the text really covers the listed items in order, write the roadmap instead ("先讲 X，再看 A、B 和 C"). This is fixed whenever found, not clustering-gated.',
      'No abstract-restatement echo ("算筹、算盘就能做加减乘除。" followed by "“会算”这件事人类很早就做到了。"): the second clause repeats the first at a higher abstraction level with zero information gain. This is the sibling of the "specifics then sweep" ban without the sweeping whole — the test is the same two-beat skeleton (concrete fact, then a quoted-or-nominalized abstraction of that same fact). Fix by keeping the concrete clause and deleting the echo, or replacing it with a fact that actually advances the text. Never wrap a common-sense statement in quotation marks or nominalize it into a concept object ("“会算”这件事") — writers do not put scare quotes around their own commonplaces. This is fixed whenever found, not clustering-gated.',
      'No "the real question is" announcer transitions ("新的问题是……", "真正的问题是……", "这就引出了一个更深的问题……", "The real question is ..."): the writer does not declare on the reader\'s behalf which question matters next; the facts themselves carry the transition, or a concrete dependency states it ("在此基础上，还剩两个障碍"). Announcing significance is narration-voice, natural when spoken in a lecture and filler once written down. This is fixed whenever found, not clustering-gated.',
      'For Beamer/PPT titles, body, captions, labels, notes, and narrative copy, no announcer transitions used only as stage announcements (including "The real question is", "A new question is", "This raises a deeper question", "Let us turn to", "新的问题是", "真正的问题是", "这就引出了一个更深的问题", and "接下来我们看"); let facts or a concrete dependency carry the transition instead.',
      'For Beamer/PPT titles, body, captions, labels, notes, and narrative copy, no unsupported hollow significance/evaluation assertions (including "this is important/significant/transformative", "this demonstrates the power/value", "意义重大", "具有重要意义", "标志着", "彰显了", "开创了", and "充分说明"); state concrete evidence, scope, or source instead, and retain significance/evaluation only when that support is stated.',
      'No rhythm-matched paired-phrase closer: do not end a sentence, paragraph, or slide with two or more neatly matched phrases ("够不够快、能不能自动", "更便宜、更安全、更可控", "faster, cheaper, better") unless each named item is a real, independently justified dimension of the claim. Matched rhythm is earned by content, not composed for cadence. If one of the parallel items would be cut without losing meaning, cut the whole flourish and state the single point plainly. This is fixed whenever found, not clustering-gated.',
      'No defensive writing in any deliverable: never pad a claim with hedging qualifiers that dilute it — 可能/或许/大概/某种程度上/在一定条件下/通常来说, "可能/possibly/perhaps/somewhat/relatively/fairly/quite", "it is worth noting that", "值得注意的是", "generally speaking", "to some extent" — unless the hedge is itself a sourced fact (measured variance, a cited confidence interval, a genuinely known scope limit). Never add self-protective filler: boilerplate disclaimers, "本文仅代表个人观点", over-attribution ("许多研究表明" without a citation), apologia for limitations nobody asked about, or a paragraph that argues against its own claim before making it. State the result and its actual scope; cut the armor.',
      'For Beamer, a single read-only visual precheck is performed by Main or task, with Main naturally selecting the one owner (never both), after task\'s initial render and before task layout; findings are advisory input to the normal task pass, then task integrates writer/zh-writer output verbatim, applies only layout changes, and renders the final revision, which Main reviews read-only as the single review owner (a task that did not produce the revision may review instead).',
      'PPTX output is optional, not automatic. When it is requested after the final validated Beamer visual revision, use the fixed external `beamer2pptx` Skill/repository at https://github.com/xdmlxdml/beamer2pptx/tree/main/beamer2pptx; do not require or invent a user-supplied conversion command or substitute another converter. Keep package validity, visual fidelity, and editability as separate claims, use OfficeCLI guidance where appropriate, do not treat OfficeCLI or a local package check as proof of Microsoft PowerPoint compatibility, and keep the task/reviewer/one-fix/fresh-evidence chain advisory with no automatic loop or hard gate.',
      'Beamer Phase 2 visual assets follow the visual workflow asset contract: official icons and assetseeker stock first, generated images opt-in with provenance recorded in the content plan; operational detail lives in latex-beamer-slides.',
    ],
  },
];

const researchWorkflows = [
  {
    id: 'research',
    chooseWhen: 'Source-backed research, web synthesis, comparison, recommendation, fact-checking, or claim-by-claim verdict.',
    skills: [
      'fact-checking',
      'claim-extraction',
      'source-evaluation',
      'citation-authenticity',
    ],
    catalogSkills: [],
    roles: [
      'fact-researcher-a',
      'fact-researcher-b',
      'fact-researcher-c',
      'fact-challenger',
      'fact-planner',
      'scout',
      'writer',
      'zh-writer',
    ],
    suggestedFlow: [
      'Decompose into checkable claims or research questions.',
      'Collect evidence from primary sources; corroborate with multiple sources.',
      'Cross-check evidence lanes for agreement, conflicts, and staleness.',
      'Route all user-facing research text through the language-matched dedicated text tool before synthesis: English goes to writer, Chinese goes to zh-writer, and mixed-language output uses separate writer and zh-writer text-tool calls.',
      'The writer/zh-writer text tool drafts the source-linked synthesis with confidence levels; Main integrates the returned text verbatim.',
      'Review verdicts for overclaiming; report limitations without drafting or rewriting prose.',
    ],
    scopeNotes: [
      'Prefer primary sources; corroborate key claims with multiple independent sources.',
      'Research roles handle evidence collection, claim extraction, and source comparison. Any user-facing prose — summaries, recommendations, verdicts, explanations, titles, captions, labels, narrative copy, and UI copy — is authored only by the language-matched dedicated text tool, keeping research prose writer-only: route English to writer and Chinese to zh-writer, using both for mixed-language output. Main only calls the text tools and integrates returned text verbatim; Main never drafts, rewrites, translates, polishes, or otherwise edits wording.',
      'Verdicts preserve exact claim tuples; compatibility evidence is not proof.',
      'The deterministic pipeline for a document-level check is fact_check_analyze -> fact_check_evidence (lane A, lane B and C only when warranted) -> fact_check_report -> fact_check_review; these four tools ship in the default tool inventory, so a natural-language check request needs no activation step.',
      'Omission defence is layered: every researcher lane also enumerates the document and returns FACT_CLAIM_CANDIDATES, fact_check_merge takes the union of those candidates (a claim only one observer lists is kept and flagged single-lane rather than dropped), and fact_check_challenge appends MISSED claims back into the plan for a fresh evidence pass.',
      'Evidence lanes get different models from the OMP agents hub (/agents), not from /model: /model sets the session model, so unbound lanes would all follow it and cross-checking would compare a model with itself.',
    ],
  },
];

const visualWorkflows = [
  {
    id: 'visual',
    chooseWhen: 'Diagrams (draw.io), UI/UX design, static visual artifacts, or rendered figure review.',
    skills: ['drawio-skill', 'assetseeker', 'frontend-design', 'canvas-design', 'format-humanizer', 'zh-format-humanizer'],
    catalogSkills: [],
    roles: ['task', 'writer', 'zh-writer'],
    suggestedFlow: [
      'Clarify the diagram type, target language, information claims, real nodes and directed relations (including conditions and grouping meaning), output paths, physical width and the destination rectangle\'s aspect ratio (4:3 or 16:9 for PPT, page-width or half-page for A4/US letter), the at most 3 key messages the figure must foreground, and any names, math symbols, or terms that must not change. Before drawing, route every user-facing English label, caption, narrative, and UI copy to the dedicated text tool writer and every Chinese equivalent to the dedicated text tool zh-writer (mixed-language output uses separate writer and zh-writer text-tool calls); task may perform only structural mapping, drawing, layout, export, and evidence-based review. For diagram-only branches, use drawio-skill (drawio@365-skills) plus the one authoring reference for the chosen method; optional asset prep happens only when the user asks for a more vivid diagram or the confirmed plan names image assets (assetseeker icons or stock; Main pre-generated via the native generate_image tool; bl image via bash for Chinese-text or photoreal nodes) with provenance recorded before drawing.',
      'After writer/zh-writer returns the copy, task derives a stable node/edge ID list from the source material, maps the text verbatim, then draws the diagram once with drawio-skill and exports: a preview PNG (no -e, longest side <= 2000), a cropped single-page vector PDF, and a final PNG (-e -s 2) repaired with the skill\'s repair_png.py. Ask the user only when the diagram meaning or physical size changes.',
      'Exactly one independent read-only review of the actual exports: Main when it has image input, otherwise one task that did not author the diagram. The reviewer checks node/edge semantics, edges crossing unrelated boxes, overlapping or collinear edges, arrow attribution, text clipping, font size at physical width, alignment, grouping margins, reading direction, gray-scale distinguishability, and the color quota (monochrome base plus at most one accent; no AI rainbow per-element or per-category color assignment); it also enforces the scientific-figure limits — node count within 5-8, at most 3 key messages dominating the canvas, labels terse, vivid icons instead of bare text boxes, and the destination frame ratio preserved in the exports; findings name node/edge IDs, location, and one executable local fix.',
      'The task applies at most one local structure/layout fix round for supported findings (keeping other node/edge IDs and positions), re-exports the same source set, and the same reviewer confirms against the fresh exports that recorded findings are resolved and no new regression was introduced; any text finding returns to writer/zh-writer instead of being edited by task or Main. This confirmation is not a second review pass or a new design round. Remaining findings are reported as limitations.',
      'Deliver the .drawio source plus the exports the task actually needs (default for paper/report: .drawio + cropped vector PDF + clean PNG); Main retains setup authorization and final acceptance only.',
    ],
    scopeNotes: [
      'drawio-skill from the 365-skills marketplace (drawio@365-skills) is the single diagram pipeline; QA is one read-only review pass plus at most one fix round and one fresh-evidence confirmation, with no repeated iteration rounds. The review is read-only and advisory; the reviewer never edits the source or the export. This plugin\'s chain does not stack the external skill\'s own user-approval loop or its self-check-fix rounds on top; the author\'s structure validation, font/box estimation, and first render belong to authoring.',
      'All user-facing visual text — node and edge labels, captions, figure annotations, narrative prose, and UI copy — must be drafted or revised by the language-matched dedicated text tool (English→writer, Chinese→zh-writer, mixed-language output uses both dedicated text tools). This text is writer-only. Main only calls the text tools and integrates the returned writer/zh-writer text verbatim; Main never drafts, rewrites, translates, polishes, or otherwise edits wording. Task only performs structure, drawing, layout, conversion, export, and evidence work; task never drafts, rewrites, authors, polishes, or changes copy.',
      'Scientific flow diagrams stay small and legible: keep the node count within 5-8 (beyond that, merge internals into grouped stages or split into clearly separated sub-figures) — never a densely packed chart; if a real system needs more nodes, prefer two simpler diagrams over one dense one. An inherited diagram that is already denser follows the 15+ autolayout rule below instead of being crammed.',
      'Labels follow real semantics: nodes are component names or short noun phrases, flow actions may be short verb-object phrases, edges carry relation/data/branch-condition names, and explanation text only when truly needed. Keep wording terse — a label is a name or a short phrase, not a sentence; when a label runs past a few words, cut modifiers or move the detail to the caption. Keep exact terms (HTTP/REST, I/O, read/write) and math symbols verbatim; never rewrite technical meaning to dodge symbols. No padding to look complete: no invented three-layer/three-stage scaffolds, fabricated gateway/bus nodes, deleted edges, or merged distinct relations for symmetry. For AI-flavor wording (staged contrasts, one-line closers, inflated significance, chatbot residue) apply the same evidence-based de-AI standard as prose. Hard label bans, fixed whenever found: no "不是X，而是Y" contrast-repetition construction or its English equivalents ("not X, but Y") in node labels, edge labels, or explanation text — state the relation once, directly; no label-colon lead-ins ("方法：…", "Step: …") inside node or edge labels; no opening or closing summary text that restates what the diagram already shows; and no defensive writing — no unsourced hedging qualifiers (可能/或许/"possibly"/"approximately" without a measured basis), no disclaimer text, no over-attribution, no apologia in explanation text. Labels state facts and relations at their actual scope.',
      'Edges connect the actual endpoints. Add an interface node only when the system really exposes an interface/bus semantics. Multiple edges to one target get distinct ports on the same side in target order with separate corridors; trunk edges follow one reading direction and feedback edges route around the outside. Orthogonal edges use edgeStyle=orthogonalEdgeStyle with explicit exitX/exitY/entryX/entryY and Array as="points" where needed — these are controls, not a global avoidance guarantee. Align same-kind nodes; size boxes to the longest label; size corridors to port count and font height instead of a fixed node-count spacing table.',
      'When editing an existing diagram: read the user-provided .drawio first, keep its node IDs and coordinate conventions, change only what was requested or found defective, and do not destroy pre-tuned ports or correct legacy errors unrequested. Run the skill\'s edgeports.py only as a pre-draft helper for edges without hand-tuned ports and before waypoints are final (it does not avoid already-occupied slots); run validate.py for structure and known paths, noting that auto-routed edges without waypoints, collinear overlaps, label collisions, and print-size font remain visual checks.',
      'For 15+ nodes or visibly dense layouts, position with the skill\'s autolayout.py (graph JSON + Graphviz) with --mono before local adjustment: give every node a suitable width/height and an explicit neutral style (--mono disables the script\'s per-group color rotation; do not use --tune to override a decided reading direction). Same source/target edges are replayed on one path by that script: keep edge identity and hand-tune their ports or outer corridors; never merge them or add fake nodes. If Graphviz is missing, fall back to a fixed-grid XML; never auto-install it or revive the retired MCP/checker pipeline. If autolayout grouping produces nested containers with relative children that export as a multi-page PDF (pdfinfo Pages > 1), flatten to top-level absolute coordinates and re-export.',
      'Size labels for the real print width, not for the canvas: use the target template figure width when known; otherwise 85 mm single-column paper, 170 mm double-column, 160 mm technical report. Target all labels (including edge and group titles) at 9 pt effective, minimum 8 pt, and size boxes with generous padding so text sits full and roomy inside its box — never cramped or clipped; computed as f_print_pt = f_canvas_px * W_print_mm * 72 / (25.4 * W_canvas_px) with W_canvas the final cropped extent including outside labels and margins. Raising PNG DPI does not fix small text: first shorten non-essential captions, reduce gaps, or move corridors; if it still does not fit, use clearly separated sub-figures of the same diagram keeping all relations, or report the constraint unmet rather than silently shrinking text or deleting content. This is this project\'s default, not a claimed journal-wide standard.',
      'Style follows paper/report norms over marketing decks: monochrome base (black/white/gray; gray steps belong to the base family, not an accent color) — white or transparent background, gray-scale box lines and text — with at most ONE additional accent color used only where it carries real semantic weight (a single highlight or a single contrast pair) — the whole diagram never exceeds three colors (black, white/gray, one accent); hard bans on the AI rainbow: per-element or per-category multi-color assignment (a different color per node, layer, or series), the colorblind-safe/dark/heatmap multi-color style presets, and gradients; line style (solid/dashed), weight, and labels carry distinctions instead of hue. When drawing, set explicit node styles (fillColor=none or white, black/gray strokes, gradientColor=none) to override the skill\'s built-in palette, and run autolayout.py with --mono (its default is per-group color rotation). No drop shadows, decorative banner titles, or a pastel per layer. Layering and symmetry come from the real structure, not a fixed three-column or three-tier template. User-provided templates or house styles win over these defaults; vector primitives are the default, and raster assets stay opt-in per the asset rules below.',
      'Asset contract: every embedded raster asset is registered with id, type (icon|photo|generated|hand-drawn), file path, and provenance (stock: source URL plus license; generated: provider, model, prompt, seed); the delivery notes list the registry and missing provenance is a review finding.',
      'Node-level asset selection prefers official icons (Iconify via assetseeker), then stock photos, then generated images; raster assets are opt-in (user request or confirmed plan) and never replace hand-drawn vector nodes wholesale. Scientific flow nodes should not sit as bare text rectangles: where a node benefits visually, embed a vivid icon or a small schematic so the box reads as a picture, not a blank label — vector glyphs first (drawn inline or from icon sets), raster assets only per the asset rules above.',
      'Generation sources: Main may pre-generate assets with the native generate_image tool and hand task the file paths; task generates directly only via bl image (bash) when Main is not preparing assets or Chinese-text rendering is required. Paid generation is proposed with a cost note before running.',
      'Scientific diagrams foreground the design novelty: identify at most 3 key messages before drawing, let the key information occupy the visual subject (largest area, central position, the single accent color), and keep supporting elements visually secondary — the figure is judged on whether its novelty reads at a glance.',
      'Compose the diagram inside an explicit destination frame: a real page/background rectangle drawn in the .drawio source with the ratio of its destination (4:3 or 16:9 for PPT slides, page-width or half-page width for A4/US-letter papers). The default export crop collapses to content bounds and would discard the ratio, so every node, label, and margin sits inside the frame rectangle and the crop follows the frame; content that cannot fit at readable sizes is simplified (fewer nodes, shorter labels), not squeezed.',
      'Embedding mechanics: the headless drawio CLI blocks file:// image loads and silently renders `image=<path>` nodes blank while still exiting 0, so a node image must be inlined as `image=data:image/png,<base64>` with no `;base64` marker (the style string splits on `;`), and an Iconify SVG\'s `fill="currentColor"` rasterizes blank unless it is first replaced with an explicit color. For an icon inline in a label box, align=left, imagePosition=left and imageWidth/imageHeight must all be set or the centered label overlaps the image; budget roughly 40 units of the text column for the icon gutter.',
    ],
  },
];

export const workflowDefinitions = defineWorkflowCatalog([
  writingWorkflows,
  researchWorkflows,
  visualWorkflows,
]);
